<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ButtonStyle" parent="Widget.AppCompat.Button.Colored">
        <item name="colorButtonNormal">@color/colorWhite</item>
    </style>

    <style name="foodRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/ratingstars</item>
        <item name="android:minHeight">0dip</item>
        <item name="android:maxHeight">15dip</item>
    </style>

    <style name="reviewRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/ratingstar_big</item>
        <item name="android:minHeight">0dip</item>
        <item name="android:maxHeight">100dip</item>
    </style>

    <style name="EditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="colorControlNormal">@color/quantum_grey500</item>
        <item name="colorControlActivated">@color/quantum_grey500</item>
        <item name="android:textCursorDrawable">@color/quantum_grey500</item>
        <item name="android:textColor">@color/colorBlack</item>
        <item name="android:textColorHint">@color/quantum_grey500</item>
        <item name="android:textColorHighlight">@color/quantum_grey500</item>
    </style>

</resources>
